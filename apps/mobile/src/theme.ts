/**
 * Mobile theme = the shared Monolith tokens (from @llamatrade/core) re-exported
 * for React Native. Font family keys match the names registered in app/_layout.
 */
export {
  palette,
  semantic,
  strategyColors,
  blockFills,
  fonts,
  radius,
  space,
  type as typeScale,
} from '@llamatrade/core/theme';
