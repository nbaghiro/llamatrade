/**
 * Marketing page URL — where the logo takes an unauthenticated visitor.
 *
 * The web app now serves the marketing page at the origin root ("/") in every
 * environment: logged-out visitors get <MarketingPage/>, signed-in users are
 * redirected to the dashboard (see apps/web/src/App.tsx `PublicHome`). So this
 * is always "/".
 */
export const MARKETING_URL = '/';
