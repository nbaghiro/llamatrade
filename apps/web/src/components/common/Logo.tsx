/** Thin re-export of Logo from the shared @llamatrade/ui design system so existing default-import call sites keep working. */
export { Logo as default } from '@llamatrade/ui';
export type { LogoProps } from '@llamatrade/ui';
